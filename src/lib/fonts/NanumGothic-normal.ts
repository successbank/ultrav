// 나눔고딕 폰트 - 경량화된 버전
// 이 파일은 한글 지원을 위한 폰트 데이터를 포함합니다
// 실제 프로덕션에서는 전체 폰트 파일을 사용하거나 CDN을 통해 로드해야 합니다

export const NanumGothicFont = {
  fontName: "NanumGothic",
  fontStyle: "normal",
  // 실제 프로덕션 환경에서는 전체 Base64 폰트 데이터가 필요합니다
  // 여기서는 플레이스홀더로 표시합니다
  fontData: ""
}

// 주의: 이 파일은 실제 폰트 데이터를 포함하지 않습니다
// 프로덕션에서는 다음 중 하나를 선택해야 합니다:
// 1. Google Fonts API를 통해 동적으로 로드
// 2. 전체 Base64 인코딩된 폰트 파일 사용
// 3. 외부 폰트 라이브러리 (예: jspdf-customfonts) 사용
