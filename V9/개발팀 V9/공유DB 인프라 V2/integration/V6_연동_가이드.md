# V6/V1 → V7 V2 연동

## 마이그레이션 순서
1. `002_pgvector.sql` 적용 (테이블 잠금 짧음)
2. embedding-worker 백필 잡 실행 (knowledge 전체 + errors 전체)
3. `003_devices.sql`, `004_sync_oplog.sql` 적용
4. `005_audit_log.sql` 적용 → 기존 변경 이력 무 (현재 시점부터)
5. `006_env_namespace.sql` 적용 → 모든 기존 행 env='prod' 디폴트
6. `007_usage_tracking.sql`, `008_rbac.sql` 적용
7. MCP 서버 v2.0.0 배포

## 호환성
- V1 도구 11개는 그대로 동작
- 신규 도구 5+ 추가
- API base path: /api/v1 유지, 신규는 /api/v2
